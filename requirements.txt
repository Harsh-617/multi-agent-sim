# Runtime dependencies — derived from pyproject.toml [project.dependencies]
# For a fully reproducible install use: pip install -e .
# For pip users without the package: pip install -r requirements.txt

numpy>=1.26,<2.0
pydantic>=2.0,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
gymnasium>=0.29
pettingzoo>=1.24
