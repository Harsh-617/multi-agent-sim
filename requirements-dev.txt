# Dev/testing dependencies — derived from pyproject.toml [project.optional-dependencies.dev]
# Install with: pip install -r requirements-dev.txt
# Or use the editable install: pip install -e ".[dev]"

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
